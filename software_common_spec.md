# 上野清掃 2.3 ソフト仕様メモ

更新日: 2026-06-21

この文書は、`uenoseisou2.3` の構成、データ保存、修正時の注意点をまとめたものです。
最初にこの文書を読めば、各ソフトの役割と計量入力の仕組みが分かるようにしています。

## 1. 基本構成

- `index.html`: 2.3のメイン画面。登録ソフトを一覧表示し、選択したHTMLを画面内で開く。
- `index_02_home.html`: ホーム表示用。主要ソフトへの入口を持つ。
- `index_smallview01.html`: 小さい画面用。主要ソフトへの入口を持つ。
- `theme.json`: 2.3共通の色設定。
- `ueno-tabbar.js`: ソフト内の共通タブ表示を補助する。
- `software_common_spec.md`: この仕様メモ。

`index.html` の `software` 配列が、2.3のソフト一覧の正本です。
主要ソフトのリンクを変更する場合は、`index_02_home.html` と `index_smallview01.html` の登録先も確認します。

## 2. 現在登録されているソフト

| 表示名 | ファイル | 役割 |
| --- | --- | --- |
| 物件表 | `index_2-3_bukken.html` | 物件情報の閲覧・管理 |
| 列選択 | `index_2-4_colview.html` | 表示列の選択 |
| 計量 3-18 | `index_3-18.html` | 最新の計量入力 |
| メモ | `index_5-1.html` | 記録用メモ |
| 縦通しカレンダー | `令和8年度収集カレンダー（縦通し）.html` | 年間の縦表示 |
| 月別カレンダー | `令和8年度収集カレンダー（月別）.html` | 月ごとの表示 |
| 燃費報告 | `index_nenpi_sousin.html` | 燃費報告メールの作成 |
| 燃費読込 | `index_nenpi_shared.html` | 燃費記録の読込 |

`index_3-16.html` と `index_3-17.html` はフォルダ内に残しているが、2.3の一覧には登録していません。物件リストの項目変更は `index_3-18.html` の `物件リスト` タブで行います。

## 3. 計量 3-18 の操作手順

1. 日付を選ぶ。
2. 担当者を選ぶ。
3. 物件リスト２列から物件名をタップする。
4. 数値を入力して「保存」を押す。
5. 自動で物件リスト２列へ戻り、次の物件を選ぶ。

物件リスト２列は入力と保存を優先した簡易画面です。物件名だけを2列で表示します。
通常の「物件リスト」は、曜日、項目、順番、物件情報の確認・編集に使います。

## 4. 物件リストと入力順

- 標準物件は44件。
- 初期状態は、物件名の読み方に基づく五十音順。
- 入力した物件は、入力した順に左上から右へ並ぶ。
- 入力済み物件の値を再入力・修正しても、順番は変えない。
- 入力順は担当者・曜日ごとに自動保存する。
- 同じ曜日を選ぶと、保存済みの順番を自動再現する。
- 履歴、履歴から作るExcel、メール本文も入力順で出力する。

漢字・英字の物件名は、`PROPERTY_NAME_READINGS` に読み方を登録して五十音順にします。
標準物件を追加・名称変更した場合は、同じ読み方表も更新します。

## 5. 入力途中の自動復元

- 当日の入力途中データは自動保存する。
- 3-18を開き直した場合、当日の入力値と入力順を自動復元する。
- 初期画面へ戻っていても、入力途中なら物件リスト２列を直接開く。
- 最初の物件を保存する前の入力値は下書きから復元する。
- 1件以上保存済みの場合は、当日の最新履歴から復元する。
- 履歴を削除すると、当日の入力値、入力順、曜日別保存順、再開情報を削除する。

履歴削除後は五十音順の初期状態へ戻ります。

## 6. 物件・項目の追加

- 設定画面の「物件を追加」から物件名、曜日、入力項目、単位を登録できる。
- 追加物件には `isCustom: true` を付けて保存する。
- 追加物件と追加項目は再読み込み後も保持する。
- 標準44件と、`isCustom: true` の追加物件だけを有効な物件として扱う。
- 古い物件や重複した保存データは、読み込み時に自動整理する。
- 通常の物件リストでは既存物件の項目追加・項目変更ができる。

追加処理を変更する場合は、3-17と3-18が同じ物件保存領域を使う点に注意し、物件項目などの設定変更は両方へ反映します。

## 7. 履歴

- 履歴の単位は「日付 + 担当者」。
- 物件を1件保存するたび、同じ日付・担当者の履歴を更新する。
- 履歴には入力値、入力順、更新日時を保存する。
- 履歴詳細は入力順で表示する。
- 履歴からExcel保存とメール本文作成ができる。
- 履歴を削除した場合、その履歴に対応する自動復元情報も削除する。

## 8. データの保存場所

計量データは、ExcelやGitHubへ直接保存するのではなく、ブラウザの `localStorage` に保存します。

主な保存キー:

| キー | 内容 |
| --- | --- |
| `keiryo_properties` | 標準物件の編集内容、追加物件、入力項目 |
| `keiryo_staff` | 担当者一覧 |
| `keiryo_order_担当者_曜日` | 担当者・曜日別の入力順 |
| `keiryo_last_物件名` | 物件ごとの前回入力値 |
| `keiryo_hist_日付_担当者` | 日付・担当者別の履歴 |
| `keiryo_hist_index` | 履歴キーの一覧 |
| `keiryo_session` | 当日の入力途中データ |
| `keiryo_mail_list` | メール送信先 |

重要:

- 保存データは、その端末・ブラウザ内にあります。
- iPhoneとパソコンのデータは自動では共有されません。
- SafariとChromeのデータも別です。
- GitHub Pagesで開いた場合と、DesktopのHTMLを直接開いた場合は保存場所が別です。
- ブラウザのサイトデータを削除すると履歴も消えます。
- GitHubへHTMLをPushしても、入力履歴はGitHubへ送信されません。

## 9. カレンダー

- 月別カレンダー: `令和8年度収集カレンダー（月別）.html`
- 縦通しカレンダー: `令和8年度収集カレンダー（縦通し）.html`
- 検索・切替用: `R8資源ごみ日程表_検索ツール.html`

月別と縦通しは当日を枠線で強調します。カレンダー固有の色を維持する必要がある場合は、`index.html` の `softwareThemeExcludes` を確認します。

## 10. 共通デザイン方針

- 白地、黒文字、黒枠を基本にする。
- 親画面の色設定を反映できる構造を維持する。
- iPhoneで文字やボタンが重ならないようにする。
- 上部タイトルとタブはスクロール時も固定し、タブ位置はタイトルの実際の高さから自動計算する。
- 操作ボタンを増やしすぎず、自動保存・自動復元を優先する。
- 入力画面は入力と保存に集中させる。
- メール送信は既存ソフトと同じ `mailto:` 方式を基本にする。

## 11. 修正時の確認項目

計量3-18を変更した場合は、最低限次を確認します。

1. 日付・担当者を選び、物件リスト２列が44件で開く。
2. 2件以上入力し、入力順に並ぶ。
3. 入力済み物件を修正しても順番が変わらない。
4. 再読み込み後に入力値と入力順が復元される。
5. 履歴詳細、Excel、メール本文が入力順になる。
6. 履歴削除後に自動復元されず、五十音順へ戻る。
7. 追加物件と追加項目が再読み込み後も残る。
8. JavaScriptの構文エラーがない。
9. `git diff --check` で問題がない。

## 12. GitHub更新

- 作業元は `/Users/annsyuu/Desktop/uenoseisou2.3`。
- GitHubリポジトリは `congfing/uenoseisou2.3`。
- 公開ブランチは `main`。
- 変更したファイルだけを記録し、関係のないファイルを含めない。
- Push後はローカルの `HEAD` と `origin/main` が同じことを確認する。
- GitHub版を確認する場合は、Push後にブラウザを再読み込みする。

## 13. 今後の更新

新しい機能、保存キー、重要な操作変更を追加した場合は、この文書も同時に更新します。
