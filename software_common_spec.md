# ソフト共通仕様メモ

今後追加するソフトは、以下の仕様を基本にする。

- メール送信は `index_3-17.html` と同じ `mailto:` 方式にする。
- デザインは白地、黒文字、黒枠を基本にする。理由は、親画面の `index.html` のカラーパレットで全体の色を変更できるようにするため。
- ボタンは色を入れず、白地、黒文字、黒枠にする。
- 押したボタン、選択中の行、選択中の状態は太字で分かるようにする。
- 履歴表示は、一覧を全部出さず、日付を選んでその日の履歴だけ表示する方式にする。
- 履歴から1件を選んだら、その1件だけをプレビューや送信対象にする。
- 新しい共通仕様が出たら、このメモに追加する。
